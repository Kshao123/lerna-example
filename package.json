{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean --yes",
    "test": "lerna run test",
    "publish": "lerna publish --no-private"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
